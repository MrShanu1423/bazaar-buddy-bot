Badiya 🔥 — ab tum ready ho **PRO VERSION** ke liye (yeh wala actually earning ke chances badhata hai 💰)

Main tumhe **upgrade code** de raha hoon jisme:

✔ Random product selection
✔ 3 products ek post me
✔ Better captions (high clicks)
✔ Stable scraping
✔ Clean formatting

---

# 🚀 PRO TELEGRAM AMAZON BOT (FINAL)

👉 Purana code delete karo ❌
👉 Ye FULL code paste karo 👇

```python
import requests
from bs4 import BeautifulSoup
import time
import random

BOT_TOKEN = "YOUR_BOT_TOKEN"
CHAT_ID = "-1003706154836"
AFFILIATE_ID = "yourtag-21"

HEADERS = {
    "User-Agent": "Mozilla/5.0"
}

URL = "https://www.amazon.in/gp/bestsellers/apparel/"

def send_message(text):
    url = f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage"
    data = {
        "chat_id": CHAT_ID,
        "text": text,
        "parse_mode": "HTML",
        "disable_web_page_preview": False
    }
    requests.post(url, data=data)

def scrape_products():
    page = requests.get(URL, headers=HEADERS)
    soup = BeautifulSoup(page.text, "html.parser")

    items = soup.select(".zg-grid-general-faceout")
    products = []

    for item in items[:15]:
        try:
            title = item.select_one("img")["alt"]
            link = "https://amazon.in" + item.select_one("a")["href"]
            price_tag = item.select_one(".p13n-sc-price")
            price = price_tag.get_text(strip=True) if price_tag else "Check price"

            affiliate_link = link + f"?tag={AFFILIATE_ID}"

            products.append((title, price, affiliate_link))
        except:
            continue

    return products

def run_bot():
    print("🚀 PRO BOT STARTED")

    while True:
        products = scrape_products()

        if len(products) >= 3:
            selected = random.sample(products, 3)

            message = "🔥 <b>TOP 3 HOT DEALS</b>\n\n"

            for i, (title, price, link) in enumerate(selected, start=1):
                message += f"""
{i}️⃣ <b>{title}</b>
💰 {price}
👉 <a href="{link}">Buy Now</a>

"""

            message += "⚡ Hurry! Deals may expire anytime!"

            send_message(message)
            print("Posted 3 products")

        else:
            print("Not enough products")

        time.sleep(1200)  # 20 minutes

run_bot()
```

---

# 🟢 Kya change karna hai

### ✅ Token

```python
BOT_TOKEN = "YOUR_BOT_TOKEN"
```

### ✅ Affiliate

```python
AFFILIATE_ID = "yourtag-21"
```

---

# 🎯 Ab tumhara bot kya karega

🔥 Ek post me 3 products
🔥 Random deals (spam nahi)
🔥 Clean formatting
🔥 Har 20 min auto post
🔥 Click chances ↑ = earning ↑ 💰

---

# 💡 PRO STRATEGY (IMPORTANT)

Sirf bot chalane se paise nahi aate — ye karo:

### 1. Channel grow karo

* WhatsApp groups me share karo
* Friends add karo

### 2. Niche fix karo

👉 Best:

* Gadgets
* Under ₹499 deals
* Student deals

---

# ⚠️ Reality check

Amazon scraping unstable ho sakta hai kabhi

👉 Agar issue aaye:

* Time delay badhao
* Multiple categories use karo

---

# 🚀 NEXT LEVEL (main bana sakta hoon)

✔ Flipkart + Amazon combo bot
✔ Auto discount detection
✔ Festival sale mode
✔ Auto trending products

---

## 🔥 Bas bolo:

👉 **“ultimate bot bana do”**

Ab tum beginner nahi — tum automation creator ho 💸🚀
